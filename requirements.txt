# Exact versions used to produce the submitted checkpoints.
# Install torch from the CUDA 12.8 index for Blackwell (sm_120) GPUs:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch
torch==2.11.0+cu128
numpy==2.4.6
pandas==3.0.3
